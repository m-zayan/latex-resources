% ============================================ packages ============================================

\usepackage{standalone}

\usepackage{mathptmx, amsmath, amsthm}

\usepackage{titlesec, geometry, lipsum, etoolbox, 
			comment, graphicx, booktabs, siunitx, pgfplotstable}

\usepackage[super]{nth}
\usepackage{hyperref}
\usepackage{parskip}

\usepackage{subcaption}

\usepackage{tikz}

\usetikzlibrary{positioning}

% ============================================= custom ==============================================

\usepackage{custom.macros}
\usepackage{custom.color}

% tikz.shape.parallelepiped
\usepackage{tikz.style.d3d}

% ===================================================================================================

\geometry{a4paper,total={215.9mm, 279.4mm}, left=25.4mm, right=25.4mm, top=25.4mm, bottom=25.4mm} 

\titlelabel{\thetitle.\quad}

% ===================================================================================================

\sisetup{
  round-mode= places,
  round-precision=2,
}

% ===================================================================================================
